#community-settings.community-settings
  header
    nonio-link(href=`/@community/admin` class="nav-item active") <span>Settings</span>
    nonio-link(href=`/@community/admin/users` class="nav-item") <span>Users</span>
    nonio-link(href=`/@community/admin/financials` class="nav-item") <span>Financials</span>
    nonio-link(href=`/@community/admin/emojis` class="nav-item") <span>Emojis</span>
  .container
    form
      .load-group(load-order="primary")
        h1 Community Settings
        label Banner
        nonio-avatar-uploader(type="banner")
      
      .load-group(load-order="secondary")
        .columns
          .column.basic-info
            h2 Basic Info
            label(for="name") Display name
            input(name="name" placeholder="Community Name" required)
            label(for="description") Description
            nonio-input(name="description" placeholder="Add a short description of your community")
            div(style="margin-top: 8px; text-align: right;")
              nonio-button Save Changes
          .column
            h2 Privacy & Permissions
            label Privacy
            nonio-radio-button-group(name="privacy" class="radio-group" value="public")
              nonio-radio-button(value="public") Public
              nonio-radio-button(value="invite-only") Invite Only

            label Who can post?
            nonio-radio-button-group(name="post_permission" class="radio-group" value="all")
              nonio-radio-button(value="all") All Users
              nonio-radio-button(value="paid") Paid Users Only
              nonio-radio-button(value="admin") Admins Only
            
            label Who can comment?
            nonio-radio-button-group(name="comment_permission" class="radio-group" value="all")
              nonio-radio-button(value="all") All Users
              nonio-radio-button(value="paid") Paid Users Only
              nonio-radio-button(value="admin") Admins Only
      

script.
  lazyload('/pages/community-settings.js', document.currentScript.closest('nonio-route'))
